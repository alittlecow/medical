<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jubo.modules.sys.dao.GoodsDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.jubo.modules.sys.entity.GoodsEntity" id="goodsMap">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="type" column="type"/>
        <result property="value" column="value"/>
        <result property="money" column="money"/>
        <result property="createTime" column="create_time"/>
        <result property="status" column="status"/>
    </resultMap>

	<select id="queryObject" resultType="com.jubo.modules.sys.entity.GoodsEntity">
		select * from goods where id = #{value}
	</select>

	<select id="queryList" resultType="com.jubo.modules.sys.entity.GoodsEntity">
		select * from goods
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from goods 
	</select>
	 
	<insert id="save" parameterType="com.jubo.modules.sys.entity.GoodsEntity">
		insert into goods
		(
			`id`, 
			`name`, 
			`type`, 
			`value`, 
			`money`, 
			`create_time`, 
			`status`
		)
		values
		(
			#{id}, 
			#{name}, 
			#{type}, 
			#{value}, 
			#{money}, 
			#{createTime}, 
			#{status}
		)
	</insert>
	 
	<update id="update" parameterType="com.jubo.modules.sys.entity.GoodsEntity">
		update goods 
		<set>
			<if test="name != null">`name` = #{name}, </if>
			<if test="type != null">`type` = #{type}, </if>
			<if test="value != null">`value` = #{value}, </if>
			<if test="money != null">`money` = #{money}, </if>
			<if test="createTime != null">`create_time` = #{createTime}, </if>
			<if test="status != null">`status` = #{status}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from goods where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from goods where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>