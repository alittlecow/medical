<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jubo.modules.sys.dao.OrderLogDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.jubo.modules.sys.entity.OrderLogEntity" id="orderLogMap">
        <result property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="oldStatus" column="old_status"/>
        <result property="newStatus" column="new_status"/>
        <result property="createTime" column="create_time"/>
        <result property="createBy" column="create_by"/>
        <result property="memo" column="memo"/>
    </resultMap>

	<select id="queryObject" resultType="com.jubo.modules.sys.entity.OrderLogEntity">
		select * from order_log where id = #{value}
	</select>

	<select id="queryList" resultType="com.jubo.modules.sys.entity.OrderLogEntity">
		select * from order_log
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from order_log 
	</select>
	 
	<insert id="save" parameterType="com.jubo.modules.sys.entity.OrderLogEntity">
		insert into order_log
		(
			`id`, 
			`order_id`, 
			`old_status`, 
			`new_status`, 
			`create_time`, 
			`create_by`, 
			`memo`
		)
		values
		(
			#{id}, 
			#{orderId}, 
			#{oldStatus}, 
			#{newStatus}, 
			#{createTime}, 
			#{createBy}, 
			#{memo}
		)
	</insert>
	 
	<update id="update" parameterType="com.jubo.modules.sys.entity.OrderLogEntity">
		update order_log 
		<set>
			<if test="orderId != null">`order_id` = #{orderId}, </if>
			<if test="oldStatus != null">`old_status` = #{oldStatus}, </if>
			<if test="newStatus != null">`new_status` = #{newStatus}, </if>
			<if test="createTime != null">`create_time` = #{createTime}, </if>
			<if test="createBy != null">`create_by` = #{createBy}, </if>
			<if test="memo != null">`memo` = #{memo}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from order_log where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from order_log where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>